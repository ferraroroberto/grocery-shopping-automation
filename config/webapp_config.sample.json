{
  "_comment": "Copy this file to webapp_config.json (gitignored) or let scripts/gen_token.py create it. Empty auth_token keeps the auth gate off. When auth_token is set, non-loopback API callers must present Authorization: Bearer <token> or ?token=<token>.",
  "host": "0.0.0.0",
  "port": 8502,
  "auth_token": "",
  "auth_password": ""
}
