[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
markers =
    e2e: end-to-end Playwright tests that drive a real browser
    live: tests that hit the real local LLM hub / whisper-server (opt-in)
